# Error Report

Displays recent errors from the database, most recent first.

## Output

JSON array of error records: `id`, `script`, `timestamp`, `message`, `stack_trace`.

## Usage

Run from script_runner. Defaults to the last 20 errors.
